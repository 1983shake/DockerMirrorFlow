version: "3.9"

services:
  dockermirrorflow:
    #image: dockermirrorflow:latest
    build: .
    container_name: dockermirrorflow
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - TZ=Asia/Shanghai