# DockerMirrorFlow
__version__ = "1.0.5"
# 1.0.5 更新：
#   - 节点获取默认 1440 分钟（24 小时）
#   - 先活体检测，再对存活节点测速
#   - 速度测试采用"固定时长下载 layer"，默认 5s，可配置
#   - 节点按速度降序排序，不再按 ping 延迟
#   - 速度测试周期默认 720 分钟（12 小时），可配置
#   - Web 页面增加速度显示
#   - 取消拉取节点候选数量限制，全部按速度顺序尝试，失败自动切换，全部失败则停止
#   - 拉取结果记录到 PullHistory（成功/失败/取消）
#   - 活体检测周期默认 60 分钟，可配置
#   - 手动获取免费节点后自动跑一次活体检测 + 速度测试
