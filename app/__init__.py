# DockerMirrorFlow
__version__ = "1.1.6"
# 1. **术语统一**：活体检测 → 在线检测，测速 → 速度测试
# 2. **拉取记录增加文件大小列**：展示每次拉取实际传输的字节数
# 3. **启动优化**：初始化任务改为后台异步执行，Web 页面秒开
# 4. **首次启动自动刷新**：前端轮询任务状态，后台任务结束后自动刷新页面
# 5. **实时进度反馈**：新增任务浮层 + 顶部进度条，展示拉取/检测/速度测试实时进度
# 6. **表格列宽自适应**：固定关键列宽，URL 列自适应剩余空间
# 7. **支持 1024 最小宽度**：全局 min-width + 横向滚动兜底
# 8. **7 天流量趋势固定高度**：不再跟随左侧栏高度，固定 240px
# 9. **搜索框位置调整**：移到"7 天流量趋势"下方，结果就地显示
# 10. **分页默认改为 5**：新增 5/10 选项，用新 localStorage key 规避旧值
# 11. **检测/速度测试按钮需先选中节点**：未选中时禁用并提示，方法层二次校验
# 12. **批量操作栏精简**：移除检测/速度测试按钮，仅保留批量禁用/启用/清空
# 13. **字体优化**：更舒适的字体栈、字体平滑、数字等宽对齐、字间距微调
# 14. **屏蔽轮询日志**：过滤 `/api/tasks/status` 的 uvicorn access 日志刷屏
# 15. **拉取记录去重（多架构镜像）**：跳过 `sha256:` 摘要 manifest 的重复记录
# 16. **拉取记录去重（HEAD 探测）**：引入待定拉取机制，纯探测不落库，5 分钟自动过期
# 17. **拉取记录丢失修复**：HEAD 和 GET 都触发待定登记，加数据库级去重兜底
# 18. **记录大小修复（关键 bug）**：原先第一个 blob 就把待定条目 `pop` 掉，导致后续字节全丢；改为 `ensure_pull_record` 复用 `pull_id` + `add_bytes_to_pull` 逐个累加
